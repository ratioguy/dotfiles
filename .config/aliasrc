# My aliases

# Neovim aliases
alias vim="nvim"

# Ls aliases
alias ll="ls -lah"
alias ls="ls --color"

# Bc alias
alias bc="bc -l -q"

# Startx as dbus-session
alias startx="dbus-run-session startx"

# rerun last command
alias !!="fc -s"

# make fdfind fd in debian
alias fd="fdfind"
